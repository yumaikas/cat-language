define aa : ('a -> 'a 'a)
{ dup }

define aaa : ('a -> 'a 'a 'a)
{ dup dup }

define aaaa : ('a -> 'a 'a 'a 'a)
{ dup dup dup }

define aaaa_2 : ('a -> 'a 'a 'a 'a)
{ aaa dup }

define pop2 : ('a 'a -> )
{ pop pop }

define pop3 : ('a 'a 'a -> )
{ pop pop pop }

define pop3_2 : ('bob 'bob 'bob -> )
{ pop pop2 }

define swapswap : ('a 'b -> 'a 'b)
{ swap swap }

define app_1 : ('A ('A -> 'B) -> 'B)
{ apply }

define app_2 : ('A ('A -> 'B) -> 'B)
{ true swap if }

define app_3 : ('A ('A -> 'B) -> 'B)
{ 42 dip pop }

define appapp : ('A ('A -> ('B ('B -> 'C))) -> 'C)
{ apply apply }

define inc_int : (int -> int)
{ 1 add_int }


